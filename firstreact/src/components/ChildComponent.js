import React from 'react'

export default function ChildComponent({title, para}){
    return(
        <div>
            <h1>{title}</h1>
            <p>{para}</p>
        </div>

        //child component
    )
} 